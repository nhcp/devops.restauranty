client/node_modules
mobile/node_modules
server/node_modules
client/.env
mobile/.env
server/.env
backend/*/.env
backend/*/node_modules
mobile/.expo
k8s/06-secrets.yaml

# Terraform local provider cache (not the lock file — that's tracked)
terraform/.terraform/
*.tfstate
*.tfstate.backup
